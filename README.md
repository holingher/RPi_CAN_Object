sudo apt install net-tools

sudo python3 -m pip install python-can
sudo python3 -m pip install cantools --break-system-packages
sudo python3 -m pip install autosar-e2e --break-system-packages

sudo python3 -m pip install distro --break-system-packages